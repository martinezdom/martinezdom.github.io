---
import SunIcon from "./icons/Sun.astro";
import MoonIcon from "./icons/Moon.astro";
import SystemIcon from "./icons/System.astro";
const { language } = Astro.props;

const THEMES = [
  { label: language.themes.light, value: "light", icon: SunIcon },
  { label: language.themes.dark, value: "dark", icon: MoonIcon },
  { label: language.themes.system, value: "system", icon: SystemIcon },
];

const buttonClasses = "rounded-full flex items-center justify-center size-11 border-2 border-indigo-500 dark:border-indigo-400 text-indigo-500 dark:text-indigo-400 bg-slate-200 dark:bg-slate-900 hover:border-indigo-950 hover:bg-indigo-950 hover:text-white hover:dark:bg-indigo-400 hover:dark:text-slate-900 hover:dark:border-slate-900 cursor-pointer transition-all duration-300";
---

<div class="fixed bottom-8 right-10">
  <button
    id="theme-toggle"
    aria-expanded="false"
    class=`${buttonClasses} relative glitter`
  >
    <span class="sr-only">{language.themes.selectorText}</span>
    
    <span id="light" class="theme-main-icon absolute">
      <SunIcon />
    </span>
    <span id="dark" class="theme-main-icon absolute">
      <MoonIcon />
    </span>
    <span id="system" class="theme-main-icon absolute">
      <SystemIcon />
    </span>
  </button>

  <div
    id="theme-menu"
    class="hidden absolute bottom-full right-0 mb-1.5 flex-col space-y-1.5"
    role="menu"
    aria-labelledby="theme-toggle"
  >
    {THEMES.map((theme) => (
      <button
        class=`theme-option ${buttonClasses}`
        data-theme={theme.value}
        role="menuitem"
        aria-label={theme.label}
        title={theme.label}
      >
        <span class="sr-only">{theme.label}</span>
        <span class="size-6">
          <theme.icon />
        </span>
      </button>
    ))}
  </div>
</div>

<script is:inline>
  const themeToggle = document.getElementById('theme-toggle');
  const themeMenu = document.getElementById('theme-menu');
  const themeIcons = document.querySelectorAll('.theme-main-icon');
  const themeOptions = document.querySelectorAll('.theme-option');
  const colorScheme = window.matchMedia('(prefers-color-scheme: dark)');

  const getSavedTheme = () => {
    const saved = localStorage.getItem('theme');
    return ['light', 'dark', 'system'].includes(saved) ? saved : 'system';
  };

  const updateActiveIcon = (theme) => {
    themeIcons.forEach(icon => {
      const isActive = icon.id === theme;
      icon.classList.toggle('opacity-100', isActive);
      icon.classList.toggle('opacity-0', !isActive);
    });
  };

  const applyTheme = () => {
    const theme = getSavedTheme();
    const isDark = theme === 'dark' || (theme === 'system' && colorScheme.matches);
    
    document.documentElement.classList.toggle('dark', isDark);
    updateActiveIcon(theme);
  };

  const setToggleState = (isActive) => {
    themeToggle.classList.toggle('bg-indigo-950', isActive);
    themeToggle.classList.toggle('text-white', isActive);
    themeToggle.classList.toggle('border-indigo-950', isActive);
    themeToggle.classList.toggle('dark:bg-indigo-400', isActive);
    themeToggle.classList.toggle('dark:text-slate-900', isActive);
      themeToggle.classList.toggle('dark:border-slate-900', isActive);
    themeToggle.classList.toggle('bg-slate-200', !isActive);
    themeToggle.classList.toggle('text-indigo-500', !isActive);
    themeToggle.classList.toggle('border-indigo-500', !isActive);
    themeToggle.classList.toggle('dark:bg-slate-900', !isActive);
    themeToggle.classList.toggle('dark:text-indigo-400', !isActive);
    themeToggle.classList.toggle('dark:border-indigo-400', !isActive);
  };

  const toggleMenu = () => {
    const isOpen = themeToggle.getAttribute('aria-expanded') === 'true';
    
    themeMenu.classList.toggle('hidden', isOpen);
    themeToggle.setAttribute('aria-expanded', !isOpen);
    setToggleState(!isOpen);
  };

  const closeMenu = () => {
    themeMenu.classList.add('hidden');
    themeToggle.setAttribute('aria-expanded', 'false');
    setToggleState(false);
  };

  const changeTheme = (theme) => {
    localStorage.setItem('theme', theme);
    applyTheme();
    closeMenu();
  };

  themeToggle.addEventListener('click', (e) => {
    e.stopPropagation();
    toggleMenu();
  });

  document.addEventListener('click', (e) => {
    if (!themeToggle.contains(e.target) && !themeMenu.contains(e.target)) {
      closeMenu();
    }
  });

  themeOptions.forEach(option => {
    option.addEventListener('click', () => {
      changeTheme(option.dataset.theme);
    });
  });

  colorScheme.addEventListener('change', applyTheme);
  document.addEventListener('astro:after-swap', applyTheme);

  applyTheme();
</script>